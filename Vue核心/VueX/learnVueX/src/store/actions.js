export default {
  //进行异步操作
  // changeName(context, payload) {   // 这列传入context（上下文），和store意义相同
  //   setTimeout(() => {
  //     context.commit('updateName')   // 不能在actions中更改state对象，只能在mutations中更改
  //     console.log(payload);
  //   }, 1000)
  // },
}